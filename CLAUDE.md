# Portugal Flight Monitor — 실행 안정성 규칙

이 프로젝트의 항공권 모니터링 조건과 출력 형식은 활성 자동화의 프롬프트와 `flight_monitoring.md`를 따른다. 아래 규칙은 조회가 중간에 끊겨도 안전하게 재개하기 위한 실행 규칙이다.

## 배치와 체크포인트

- 리스본 왕복 24개와 오픈조 24개를 한 번의 긴 브라우저 호출로 처리하지 않는다.
- 브라우저 자동화 호출 한 번에는 최대 4개 날짜 조합만 처리한다. 제한시간에 가까워지면 더 작은 배치로 줄인다.
- 각 날짜 조합을 완료할 때마다 `.monitoring_checkpoint.json`에 즉시 저장한다.
- 새 실행에서 미완료 체크포인트가 있으면 완료된 조합은 건너뛰고 실패·미완료 조합부터 재개한다.
- 체크포인트가 완료 상태이거나 시작 후 12시간이 지난 경우에만 새 전체 조회를 시작한다.

## 네이버 결과 검증

- 페이지가 열린 뒤 항공편 카드 수와 상위 가격을 2초 간격으로 확인한다. 최소 6초 이상 기다리고 카드 수와 상위 가격 목록이 3회 연속 같을 때 로딩 완료로 판단하며, 최대 30초까지 확인한다.
- 로딩이 끝나지 않으면 해당 날짜 조합만 새 탭에서 한 번 재시도한다.
- 오픈조는 여정 1과 여정 2를 모두 선택한 뒤 최종 조합 가격과 결제수단이 표시돼야 완료로 처리한다.
- 첫 구간 선택 화면의 `전체` 가격을 오픈조 최종가로 사용하지 않는다.
- 예약 링크는 날짜 검색 결과 목록 URL을 사용하지 않는다. 가는 편과 오는 편을 실제로 선택한 뒤 생성되는 `/detail/` 및 `selectedFlight`가 포함된 상세 URL을 그대로 복사해 기록한다.
- 상세 URL을 다시 열어 기록한 편명과 선택된 항공편이 일치하는지 확인한 경우에만 해당 날짜 조합을 완료로 처리한다. 직항·경유·오픈조 모두 동일하게 적용한다.
- 실적·상품·로그인 등 추가 조건이 없는 카드 중 최저가가 같은 카드가 여러 개면 최대 3개까지 `/`로 연결해 표시한다. 2개이면 `우리/현대카드`, 3개이면 `현대/삼성/우리카드`처럼 반복되는 `카드`를 마지막 이름에만 붙인다. 4개 이상이면 세 번째 이름 뒤에 말줄임표를 붙여 `현대/삼성/우리…`처럼 표시한다. 단독 최저가는 해당 카드 하나만 표시하며 `IBK기업은행카드`는 `기업은행카드`로 표시한다.
- 일반결제 또는 모든 결제수단 가격까지 카드 최저가와 같을 때만 카드명 대신 `모든 결제수단 동일가`를 표시한다.
- 배치가 끝날 때마다 사용한 탭을 정리한다.

## 게시 안전성

- 48개 날짜 조합이 모두 완료되고 결과 검증을 통과하기 전에는 `flight_monitoring.md`의 본문과 `마지막 추적`을 바꾸지 않는다.
- 부분 결과로 현재 최저가, 조건 충족 개수, 가격 이력을 변경하지 않는다.
- 일부 조합이 실패하면 기존 공개 결과를 유지하고 체크포인트를 남겨 다음 실행에서 이어간다.
- 전체 완료 후에만 `flight_monitoring.md`를 갱신하고 게시하며 체크포인트 상태를 `complete`로 변경한다.
- `.monitoring_checkpoint.json`은 로컬 전용이며 GitHub에 게시하지 않는다.
